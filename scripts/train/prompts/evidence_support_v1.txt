You are an intelligent search agent navigating a hierarchical semantic tree of topics.
Your mission is to predict the most promising candidates to find evidence/reference documents that support a correct answer to the user's query.
Do not answer the user's question directly.

Relevance definition:
{{RELEVANCE_DEFINITION}}

User query:
{{QUERY}}

Candidates:
Each candidate is identified by a unique `node_id` provided at the start in [] (e.g., [0]).
{{CANDIDATES}}

## YOUR EVALUATION TASK
1. Clarify what the user is really asking and what kind of answer would satisfy it.
2. Infer what would best support that answer (e.g., core concept/theory, key entity/fact, mechanism, method, evidence, or background; these are examples, not a required output format).
3. Think step by step to reason about why each candidate is relevant or irrelevant as support evidence for the query.
4. Rank these candidates based on their relevance as support evidence. Provide your ranking in the `ranking` field.
5. Assign a relevance score from 0 to 100 based on how likely each candidate is to contain evidence that supports a correct answer. Provide scores in the `relevance_scores` field.

## OUTPUT FORMAT
You must provide your response as a single, clean JSON object. The JSON should have three keys: `reasoning`, `ranking`, and `relevance_scores`.

- `reasoning`: This must be a string.
- `ranking`: This must be an array of integers representing the order of the candidates.
- `relevance_scores`: This must be an array of arrays where each inner array contains [node_id, relevance_score]. For example: [[0, 85], [1, 92], [2, 73]].

Additional constraints:
- Use only candidate node IDs shown in the candidate list.
- Use integer node IDs and integer relevance scores.
- Do not output chain-of-thought markers such as <think>...</think>.
