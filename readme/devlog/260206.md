  ## 2026-02-06 Brainstorm Plan: Corpus-Grounded Dynamic Categories (Safe Explore)

  ### Summary

  - 목표: 고정 4카테고리(Theory/Entity/Example/Other) 병목을 줄이되, 완전 open-set의 드리프트를 피한다.
  - 핵심 전략: 코퍼스에서 category 후보를 미리 구축하고, 매 iteration에서 RAG-for-category로 소수 후보만 선택해 사용한다.
  - cross-domain miss(예: bee movement -> Poisson) 대응: query-only가 아니라 3-Query Union + Global Backoff.
  ### Decisions Locked

  - explore 정책: Keep+Add (이전 지지 top category 유지 + 새 category 추가)
  - category 후보 사용 방식: RAG-for-category
  - cross-domain 보강: 3-Query Union
      - 신호 1: original/current query
      - 신호 2: current evidence summaries
      - 신호 3: LLM possible_answer_docs
  - LLM에 직접 보여줄 후보 개수: 기본 Top 32 pool에서 최종 8~12개만 노출

  - In scope:
      - category inventory 생성 파이프라인
      - round3 runtime에서 category retrieval/selection 모듈
      - explore/exploit 규칙 변경(keep+add, backoff)
      - 로그/평가 지표 추가
      - readme/devlog/260206.md 브레인스토밍 문서화
      - full open-set 자유명명 운영
      - 기존 모델/리트리버 교체

  ### Public Interfaces / Config Changes

      - --round3_category_inventory_path (jsonl/json)
      - --round3_category_rag_topk (default: 32)
      - --round3_category_prompt_topk (default: 10)
      - --round3_explore_add_k (default: 1)
      - --round3_category_backoff_k (default: 1)
      - --round3_category_conf_threshold (low-confidence cutoff)
  - src/run_round3_1.py:
      - fixed CATEGORY_ORDER 의존 경로를 “inventory 후보군” 기반으로 확장
      - category selection 결과를 query_categories, category_candidate_pool, category_selection_reason, backoff_applied로 기록
  - 신규 스크립트:
      - scripts/build_category_inventory.py (코퍼스 사전 구축)
      - 기존 분석 스크립트 확장 또는 신규:
          - explore keep ratio
          - backoff activation ratio
          - cross-domain jump hit proxy


  2. category candidate retrieval:
      - 3-Query Union으로 inventory 검색
      - 합집합 후보를 점수 정규화 후 top rag_topk 유지
  3. low-confidence 판단:
      - max score < threshold 또는 score gap 작으면 global_prior에서 backoff_k 추가
  4. prompt/selector 단계:
      - 후보 중 prompt에는 prompt_topk만 노출
      - exploit: top supported category 중심
      - explore: prev_top1 유지 + 새 top1 추가(add_k=1)
  5. selected categories로 rewrite query 구성 후 Iter X+1 진행


  - 입력: subset corpus documents
  - 산출물 스키마(예시):
      - category_id, label, aliases, definition, centroid_emb, prior, source_stats
  - 구축 방식:
      - 문서 키워드 추출(tf-idf/KeyBERT/NP chunk 중 1개 고정)
      - 유사 키워드 클러스터링으로 category label 생성
      - 너무 세분화된 군집은 merge, 너무 일반적인 군집은 drop
  - 주의:
      - category 수 상한 설정(예: 200~500)
      - 중복/동의어 정리 규칙 명시


      - inventory 파일 없을 때 명확한 에러
      - inventory 있을 때 category 후보 생성/선택 로그 정상 기록
      - explore 시 항상 이전 top category 포함(keep+add invariant)
      - low-confidence 상황에서 backoff가 실제 발동
  - 회귀 테스트:
      - 기존 round3_action_v1 + soft_keep=1 baseline 재실행 가능
      - category selector off 시 기존 동작 동일
  - 성능/분석:
      - explore keep ratio 보고서 생성
      - cross-domain 케이스셋에서 miss 감소 여부 확인
  ### readme/devlog/260206.md 문서 구성안

  - 제목: # 2026-02-06 브레인스토밍: Corpus-grounded Dynamic Category
  - 섹션:
      - 문제정의 (고정 4카테고리 병목)
      - 설계 결정 (Keep+Add, 3-Query Union, Global Backoff)
      - Iteration 알고리즘
      - 실험 계획 (ablation matrix)
      - 리스크/완화 (category drift, cost, noise)
      - TODO 체크리스트

  ### Assumptions / Defaults

  - 기본 정책은 안전성 우선(precision보다 miss 방지 균형).
  - inventory는 subset별 독립 구축.
  - 초기에 prompt_topk=10, rag_topk=32, backoff_k=1, explore_add_k=1로 시작.
  - cross-domain 보강은 query-only 금지, 3-신호 합집합 고정.

## 2026-02-08 Category Assignment v2 (brief)

- 파일: `scripts/document_expansion_role.py`
- 변경 목적:
    - 문서 1개가 쿼리 정답에 기여하는 수단이 하나가 아닐 수 있으므로, category keyword를 multi-label로 확장.
    - chunk 문서 id 정규화 오류(`...숫자_숫자`, `...숫자`)를 줄여 문서 단위 병합 정확도 개선.

- 방법 요약:
    - 2-level category keyword 생성 유지:
        - level1: 역할 타입(Theory/Rule/Method/Evidence/Entity/Resource/Example/Background)
        - level2: 재사용 가능한 추상 카테고리(2~4 words)
    - 문서당 다중 키워드 허용:
        - 출력 필드 `category_level_1_keywords`, `category_level_2_keywords` 추가
        - 역호환을 위해 `category`는 primary level2 유지
    - 프롬프트에 doc id를 본문 snippet에도 명시(` [DOC_ID] ...`)해서 LLM이 문서 단위를 안정적으로 식별하도록 함.
    - 병합 로직:
        - strong pattern: `...<group>_<chunk>.txt`
        - weak pattern: `...<group>.txt` (family evidence 있을 때만 병합)

- 기대 효과:
    - retrieval 신호가 단일 라벨에 눌리지 않고, 한 문서의 복합 역할(예: Theory + Evidence)을 보존.
    - chunk 병합 누락/오병합 감소로 문서 단위 category 부여 품질 개선.
